export async function GET() {
  // TODO(Phase 3, see docs/tasks/authorization.md): exchange the email
  // confirmation code for a session via the Supabase server client, then
  // redirect into the app.
  return new Response("Not implemented", { status: 501 });
}
